import 'package:flutter/material.dart';

class ColorOption {
  const ColorOption({
    required this.name,
    required this.value,
  });

  final String name;
  final Color value;
}

class RadiusOption {
  const RadiusOption({
    required this.name,
    required this.value,
  });

  final String name;
  final double value;
}

class ElevationOption {
  const ElevationOption({
    required this.name,
    required this.value,
  });

  final String name;
  final double value;
}
